name: whiteboard_demo
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^1.0.3
  provider: ^6.0.1
  flutter_colorpicker: ^0.6.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  json_serializable: ^6.0.1
  json_annotation: ^4.3.0

  

flutter:
  uses-material-design: true
